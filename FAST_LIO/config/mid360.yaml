common:
    lid_topic:  "/livox/lidar"
    imu_topic:  "/livox/imu"
    # imu_topic:  "/mavros/imu/data"
    time_sync_en: false         # ONLY turn on when external time synchronization is really not possible
    time_offset_lidar_to_imu: 0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
                                  # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0

preprocess:
    lidar_type: 1                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 
    scan_line: 6
    blind: 0.4

mapping:
    acc_cov: 0.1
    gyr_cov: 0.1
    b_acc_cov: 0.001
    b_gyr_cov: 0.001
    fov_degree:    120
    det_range:     450.0
    extrinsic_est_en:  true      # true: enable the online estimation of IMU-LiDAR extrinsic
    extrinsic_T: [ -0.011, -0.02329, 0.04412]
    extrinsic_R: [ 1, 0, 0,
                   0, 1, 0,
                   0, 0, 1]

lidar_to_center:  # 雷达安装位置偏移(m)（以雷达为原点的中心坐标 X轴向前 Z轴向上）
    lidar_translation_x: 0.0
    lidar_translation_y: 0.0
    lidar_translation_z: 0.0

publish:
    path_en:  true
    scan_publish_en:  true        # false: close all the point cloud output
    dense_publish_en: true        # false: low down the points number in a global-frame point clouds scan.
    scan_bodyframe_pub_en: true   # true: output the point cloud scans in IMU-body-frame
    effect_world_pub_en: false    # true: 发布局部系点云
    laser_cloud_map_pub_en: false # true: 发布地图
    HMI_odom_pub_en:  true        # true: 向上位机界面发布里程计信息

pcd_save:
    pcd_save_en: false
    body_pcd_save_en: true
    pose_save_en: true
    interval: -1                 # how many LiDAR frames saved in each pcd file; 
                                 # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
